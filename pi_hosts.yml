all:
  hosts:
    pi-server:
  children:
    magicmirror:
      hosts:
        pi-server:
          ansible_ssh_user: pi
      vars:
        ansible_python_interpreter: /usr/bin/python
        proxy_env: {}
